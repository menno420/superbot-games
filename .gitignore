# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
wheels/
pip-wheel-metadata/

# Virtual environments
.venv/
venv/
env/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
.tox/

# Editors / OS
.vscode/
.idea/
.DS_Store

# Local env
.env
.env.*

# substrate-kit: the per-adopt bootstrap backup is a redundant full copy of the
# vendored root bootstrap.py — don't commit it (the live state/telemetry under
# .substrate/ IS committed, per the kit's own convention).
.substrate/backup/

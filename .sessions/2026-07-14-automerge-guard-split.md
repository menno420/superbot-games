# 2026-07-14 · automerge guard split — enabler host customizations made regen-proof (build)

> **Status:** ✅ `complete`
>
> 📊 Model: Fable-class · high · feature build

Mirror of idle PR #137 (squash `8ff9f59`), carrying THIS repo's exact
current values. The live `.github/workflows/auto-merge-enabler.yml`
(@ `688cbf1`) is kit-owned but hand-carries three host customizations
(17-prefix branch allowlist · in-progress/drafted card-skip step ·
`Head-ref:` squash-body provenance). Every kit regen banks + clobbers
them — the v1.16.0 upgrade (#141) banked
`.substrate/backup/auto-merge-enabler.pre-regen-71045852.yml` and the
session restored the file by hand — and `check --strict` prints the
`automerge-branch-drift` advisory (workflow arms 17 prefixes while
config would regenerate `{claude/*}`). The split, per the kit template's
own doctrine ("Put host-specific customizations in a SEPARATE workflow
file, never in this one"):

- branch allowlist → `substrate.config.json` `automerge.branch_patterns`
  (all 17 live prefixes, verbatim, regen-preserved; drift advisory
  quieted);
- `auto-merge-enabler.yml` → regenerated byte-identical to the kit
  v1.16.0 template output for those patterns
  (`automerge_enabler_workflow(*_automerge_params(config))` == live
  file, so future regens are no-ops);
- card guard + Head-ref provenance → new host-owned
  `.github/workflows/automerge-card-guard.yml`, a reconciler modeled on
  idle's post-#137 guard: disarms while an in-diff `.sessions/*.md` card
  is `in-progress`/`drafted` (this repo's #40 premature-merge incident
  class), re-stamps `Head-ref:` into the pending squash body when armed
  with final cards, never arms a PR from scratch.

**Known merge-safe delta** (idle #137's documented observation, carried
here): at PR open the kit enabler arms card-blind for ~1–2 minutes until
the guard's disarm lands — the merge stays blocked throughout because
the required substrate-gate holds born-red PRs red; the born-red card
(this file) is what holds the merge floor, exactly as designed.

Landed as PR #142 (`claude/automerge-guard-split`); the PR proved the
guard live pre-flip (arm-then-disarm on the born-red card — run results
recorded in the PR body/report). Outbox KIT-ASK appended
(`control/outbox.md` § "KIT-ASK · automerge card guard as a kit knob"):
promote the guard to an `automerge.card_guard` kit knob, citing idle
#137 (`8ff9f59`) + this PR as the two live implementations.

Verified at flip time on this head: `python3 -m pytest -q` = **810
passed** (exit 0, baseline-identical) · `python3 tools/preflight.py` =
GREEN, all three gates · `python3 bootstrap.py check --strict` =
`automerge-branch-drift` advisory GONE (present at `688cbf1`
pre-change); pre-flip exit 1 solely from this card's designed born-red
hold. Enabler regen-proof: `automerge_enabler_workflow(
*_automerge_params(config))` == live file → True; the vendored template
applied to idle's 4 patterns reproduces idle's post-#137 enabler
byte-for-byte, and games' new enabler differs from idle's on exactly
one line (the config-baked allowlist). This flip commit also releases
the work claim (`control/claims/automerge-guard-split.md`) per the
same-branch-delete precedent (#106/#107, eap-order-009 flip) and
commits the accumulated `.substrate/guard-fires.jsonl` delta.

## 💡 Session idea

The guard's job-level `if:` allowlist is a HAND-COPY of
`substrate.config.json` `automerge.branch_patterns` (its own comment
says "keep the two lists in sync") — the exact drift class the kit's
`automerge-branch-drift` advisory just caught for the ENABLER is now
reproducible, unwatched, in the guard. Extend that advisory: any host
workflow carrying a marker comment (e.g. `# sync:
automerge.branch_patterns` above its `if:`) gets its rendered
`startsWith(...)` expression parsed back to a pattern set and compared
to config, advisory on mismatch. Dedupe against recent card ideas:
nearest neighbors are the #139 block-sync idea (byte-identical prose
blocks across files — this is config→rendered-expression equivalence,
not byte identity) and the kit's existing enabler-only drift advisory
(kit PR #321 — reads ONLY `AUTOMERGE_ENABLER_RELPATH`, never host
workflows); neither covers a host workflow's derived expression. If the
outbox KIT-ASK (`automerge.card_guard` knob) ships, the kit renders the
guard from config and this advisory extension becomes its regen check.

## ⟲ Previous-session review

Target: superbot-idle PR #137 (squash `8ff9f59`,
`.sessions/`-equivalent split this session mirrors) — re-verified its
load-bearing claims from THIS repo's vendored kit before trusting them:
its "regenerated byte-identical to the kit v1.16.0 template output"
claim reproduces exactly (games' `bootstrap.py`
`automerge_enabler_workflow(["claim/*","claude/*","close-out/*",
"control/*"], "substrate-gate")` == idle's live enabler file,
byte-for-byte — two independently vendored kits agree on the template);
its guard proved itself live on its own PR (merged BY
`github-actions[bot]` at 2026-07-14T18:17:14Z, i.e. the flip re-arm
landed the squash, API-verified); and its documented ~1–2 min
card-blind arming window is honest — idle's guard waits bounded
(36×5 s) on the enabler's completed verdict before reconciling. One
carried-forward nit: idle's guard header says "4-prefix branch
allowlist" in prose; the number is per-host and already wrong for any
adopter with a different set — this repo's copy says 17 and will rot
the same way if the allowlist grows (the 💡 above is the structural
fix).

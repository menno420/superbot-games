# 2026-07-14 · automerge guard split — enabler host customizations made regen-proof (build)

> **Status:** `in-progress`
>
> 📊 Model: Fable-class · high · feature build

Mirror of idle PR #137 (squash `8ff9f59`), carrying THIS repo's exact
current values. The live `.github/workflows/auto-merge-enabler.yml`
(@ `688cbf1`) is kit-owned but hand-carries three host customizations
(17-prefix branch allowlist · in-progress/drafted card-skip step ·
`Head-ref:` squash-body provenance). Every kit regen banks + clobbers
them — the v1.16.0 upgrade (#141) banked
`.substrate/backup/auto-merge-enabler.pre-regen-71045852.yml` and the
session restored the file by hand — and `check --strict` prints the
`automerge-branch-drift` advisory (workflow arms 17 prefixes while
config would regenerate `{claude/*}`). The split, per the kit template's
own doctrine ("Put host-specific customizations in a SEPARATE workflow
file, never in this one"):

- branch allowlist → `substrate.config.json` `automerge.branch_patterns`
  (all 17 live prefixes, verbatim, regen-preserved; drift advisory
  quieted);
- `auto-merge-enabler.yml` → regenerated byte-identical to the kit
  v1.16.0 template output for those patterns
  (`automerge_enabler_workflow(*_automerge_params(config))` == live
  file, so future regens are no-ops);
- card guard + Head-ref provenance → new host-owned
  `.github/workflows/automerge-card-guard.yml`, a reconciler modeled on
  idle's post-#137 guard: disarms while an in-diff `.sessions/*.md` card
  is `in-progress`/`drafted` (this repo's #40 premature-merge incident
  class), re-stamps `Head-ref:` into the pending squash body when armed
  with final cards, never arms a PR from scratch.

**Known merge-safe delta** (idle #137's documented observation, carried
here): at PR open the kit enabler arms card-blind for ~1–2 minutes until
the guard's disarm lands — the merge stays blocked throughout because
the required substrate-gate holds born-red PRs red; the born-red card
(this file) is what holds the merge floor, exactly as designed.

# 2026-07-13 · V043 economy surfacing — balance page section + fishing CLI sell/level (feature build)

> **Status:** ✅ `complete`
>
> 📊 Model: Fable · 2026-07-13T18:01:04Z · v043-balance-cli

Surfaced the just-wired V043 fishing economy (PR #83, merge `72a94bb`) on the
two player/owner-visible surfaces that could not see it (PR #87):

- **`tools/gen_balance.py`** — a "Fishing economy (V043)" section: the sell
  curve (`SELL_VALUES`), the progression curve (`xp_to_next(L) = 50·L` via
  `XP_PER_LEVEL`), and the L10/L25 milestones, ALL imported/derived at
  generation time from `games/fishing/core/economy.py` — zero hand-copied
  literals, so the page cannot drift from the sim pins. `docs/balance.md`
  regenerated; byte-for-byte drift check re-run.
- **`games/fishing/cli.py`** — a `sell` command routed through the audited
  `services/fishing_workflow.sell` (the SINGLE mutation path — the CLI adds
  no economy logic) + coins / level / milestone readouts in the status header
  and cast/sell output. Tests in `tests/fishing/test_cli.py` style; suite
  floor ratcheted.
- **`docs/current-state.md`** — groom recording the merged #81–#86 wave
  (each merge API-verified with its squash SHA); the stale "awaiting
  implementation: V042–V045" claim corrected to served.

Evidence: suite 546 → 556 (`pytest -q` — 556 passed; `tests/fishing` floor
94 → 104); `tools/gen_balance.py --check` clean, second fresh regen `cmp`'d
byte-for-byte against the committed page (exit 0).

## 💡 Session idea

The V043 exclusivity mechanism is live but half-armed: `sell` consumes the
fish from the haul so a future **cook** leg can never double-spend it — yet
no cook leg exists and, unlike every other economy lever in this repo, it has
NO sim-pinned constants waiting (VERDICT 043 wired sell only). File the
follow-up SIM-REQUEST in `control/outbox.md`: cook-leg pricing (what a cooked
fish is worth — coins? energy restored, mirroring mining's rations
faucet-bypass question in VERDICT 042's flag? — per-species or flat), so the
second leg can be wired VERBATIM the day a verdict lands instead of stalling
a build session on invented numbers. Dedupe check: today's cards reference
"sell-OR-cook" only as the exclusivity note; no card or open SIM-REQUEST
covers sourcing the cook constants (the four prior SIM-REQUESTs all closed
via ORDER 007).

## ⟲ Previous-session review

The previous session (PR #83, merge `72a94bb` — the V043/V044 verdict
fan-out) made this session cheap in a way worth naming: it put the xp/level/
milestone surfacing ON the seam's `FishingResult` (`xp_gained`,
`level_after`, `milestones`) instead of leaving derivation to callers — so
this session's CLI display is pure formatting with zero economy logic, and
the balance-page section could import one pure module (`economy.py`) rather
than scrape the seam. One rough edge it left: `FishingState.coins` and
`MiningState.coins` are separate, unconnected balances — fine at rung 2, but
a one-wallet-or-many decision will be needed before any rung-3 host binding.

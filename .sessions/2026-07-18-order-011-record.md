# 2026-07-18 · order-011-record — control(inbox)+docs: record ORDER 011 (owner live 2026-07-18, most-recent-wins)

> **Status:** `in-progress`
>
> 📊 Model: Claude Opus 4.x · medium · docs/records

⚑ Order-driven: owner-authorized routine task (menno420, live 2026-07-18). The
owner's live 2026-07-18 chat direction is leading and most-recent-wins:
"continue, do not let anything stall". The `control/` bus still carries a
2026-07-17 wind-down / fresh-start note (RETIREMENT NOTICE in `control/inbox.md`;
FRESH-START CLEANUP banner in `docs/current-state.md`) plus earlier
pause-the-loop guidance (ORDER 010 — "do NOT re-arm routines"). Those predate
the owner's 2026-07-18 direction, so a later session could read them as current
and stall the seat. This slice records the correction: it appends an
append-only `## ORDER 011` entry to the END of `control/inbox.md` and inserts a
single dated pointer line directly ABOVE the 2026-07-17 note in
`docs/current-state.md` (nothing removed). Records-only (+ this card + claim);
no code.

**What this slice records (all verified on main before citing).** The
2026-07-17 wind-down note and any earlier pause-the-loop guidance are
**superseded for seat operations** by the owner's live 2026-07-18 direction
(most-recent-wins). The seat work loop is **resumed** under standing mandate
from ORDER 048. The Projects EAP read-only date (**2026-07-21**) stays a
platform fact to re-verify on the day — unchanged by this order. Nothing in the
inbox above ORDER 010 is touched (inbox is append-only), and the 2026-07-17
FRESH-START CLEANUP note in `current-state.md` is preserved byte-for-byte with
the new pointer line inserted only above it.

Scope: `control/inbox.md` (one append-only `## ORDER 011` block at the END),
`docs/current-state.md` (one dated pointer line inserted directly above the
2026-07-17 note), `control/claims/2026-07-18-order-011-record.md`, and this card.

## 💡 Session idea

⚑ Order-driven. A durable, timestamped record beats an ephemeral chat turn: the
owner's "most recent info wins" direction lives in a session chat, but the
control bus and living ledger are what the NEXT session actually reads on wake.
The idea worth keeping — when a live owner turn supersedes a written note,
record the supersession AT the stale note (a pointer line directly above it),
not only in a fresh order far away, so the reader who lands on the old note is
handed its correction in the same glance rather than having to discover a newer
order elsewhere.

## ⟲ Previous-session review

Predecessor: the 2026-07-18 `unstale-human-gated` session (card
`.sessions/2026-07-18-unstale-human-gated.md`, PR #165) — a docs-truth slice
that corrected `NEXT-TASKS.md`'s stale "merges are human-gated" line against the
living ledger. That review remark applies directly here: it flagged that a
reference doc silently teaching a retired rule is worse than a missing one. This
slice extends the same discipline to the wind-down note — rather than deleting
the 2026-07-17 note (which stays true as provenance), it stamps the supersession
in place so the retired operating rule cannot be read as current.

## ✅ Landed

_Pending final flip — filled with the PR number and check results once the
records land on green via the live auto-merge apparatus._

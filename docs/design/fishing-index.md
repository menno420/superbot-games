# Fishing — design index

> **Status:** `reference`
>
> Per-domain design index for the fishing lane. Linked once from
> `docs/current-state.md` so adding a fishing design doc edits only THIS file, not the
> shared current-state ledger. The docs-gate reachability BFS follows the links below,
> so every doc listed here stays reachable through this index.

- [`fishing-catch-skeleton.md`](fishing-catch-skeleton.md) — fishing walking skeleton:
  pure catch resolver, reuses mining's energy/gear/determinism substrate.

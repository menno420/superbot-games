"""superbot-games — game domain plugins.

Each game (mining, exploration, …) is a self-contained plugin package that
docks onto the superbot-next host contract. See ``docs/lanes.md`` for the
per-Project ownership split and ``docs/design/mining-plugin-layout.md`` for the
mining plugin's three-layer shape.
"""

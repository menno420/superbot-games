# claim

- `claude/games-hub-registry` · **build** — in-repo game hub: a game-neutral `services/world_registry.py` (opener-as-opaque-callable `WorldEntry`) + a `python -m games` launcher (`games/__main__.py`) that registers and dispatches both playable games (mining, fishing) · expected files: `services/world_registry.py`, `games/__main__.py`, `games/registry_wiring.py`, `services/tests/test_world_registry.py`, `services/tests/EXPECTED_MIN_TESTS.txt` · 2026-07-13

# Claim · bug fix — games/dnd resolver TypeError on unhashable option_id

- `claude/night-fix-dnd-resolver-unhashable` · **night-fix-dnd-resolver-unhashable** — fix the one documented clamp-law violation: `games/dnd/core/resolver.py:100` does `dm_choice.option_id in allowed_ids`, so a hand-built `DMChoice(option_id=['a','list'])` raises `TypeError: unhashable type: 'list'` instead of clamping; owed fix per docs/retro/close-out-world-games-2026-07-11.md:38 ("fold a 1-line try/except clamp … into the next resolver-touching slice"); minimal try/except → existing clamp branch + fuzz-suite extension with unhashable payloads (list/dict/None/int) · expected files: games/dnd/core/resolver.py, tests/dnd/, tests/dnd/EXPECTED_MIN_TESTS.txt, docs/balance.md, .sessions/, telemetry/model-usage.jsonl, control/claims/ · 2026-07-14

# Claim · test — detector-trip registry: every sim invariant predicate has a proven violation witness

- `claude/night-detector-trip-registry` · **night-detector-trip-registry** — test slice implementing the detector-trip registry card idea from `.sessions/2026-07-14-night-coverage-economy-sim.md`, verified unimplemented at main `fefe16c` (no registry module exists; #99's tests construct violations for economy_sim's three invariants but nothing enumerates predicates across sims or REQUIRES a witness per predicate): new test module in the registered `tests/shared/sim/` suite that derives the set of bool-returning invariant predicates from the sim modules themselves (annotation-based enumeration, not hand-listed), keeps a witness registry mapping each predicate to a constructed violating input, asserts registry keys == enumerated predicates (a witness-less predicate fails; a stale witness for a removed predicate fails), and asserts each witness actually makes its predicate return False · expected files: tests/shared/sim/test_detector_trips.py, tests/shared/sim/EXPECTED_MIN_TESTS.txt, docs/balance.md (floor bump regen), .sessions/, telemetry/model-usage.jsonl, control/claims/ · 2026-07-14

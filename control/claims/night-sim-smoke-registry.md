# Claim · test — sim-harness smoke registry: every sim's render path executes in CI

- `claude/night-sim-smoke-registry` · **night-sim-smoke-registry** — implement the sim-harness smoke registry from `.sessions/2026-07-14-night-coverage-encounters-sim.md`'s card idea (verified unimplemented; this exact gap let #106's `format_report` ZeroDivisionError stay latent): one parametrized registry of (sim module, tiny sweep kwargs) covering `games/mining/sim/encounters_sim`, `games/shared/sim/economy_sim`, `games/fishing/sim/catch_sim`, `games/dnd/sim/menu_sim` and `games/exploration/survival/sim`, asserting `format_report(run(**tiny_kw))` returns non-empty text without raising, plus a glob-derived completeness check that every `*_sim.py` under games/ is enumerated so new sims can't dodge it · expected files: tests/shared/sim/test_sim_harnesses.py, tests/shared/sim/EXPECTED_MIN_TESTS.txt, docs/balance.md, .sessions/, telemetry/model-usage.jsonl, control/claims/ · 2026-07-14

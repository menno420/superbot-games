# Claim · UX fix — hub prints "Launching X…" after the game exits

- `claude/night-fix-hub-launch-order` · **night-fix-hub-launch-order** — fix the hub launch-banner order: `games/__main__.py:146` returns `lines=["Launching {id}…"]` AFTER `_default_launch` has already run the game synchronously, and `main()` prints `step.lines` only after `hub_step` returns, so `printf 'play mining\nquit\nquit\n' | python3 -m games` shows the banner after mining's session summary; move the banner emission into the launch path so it precedes the game session, keep `hub_step`'s dispatch contract and the injectable-launch audit seam, update #111's `services/tests/test_games_hub_loop.py` expectations to pin the NEW order · expected files: games/__main__.py, services/tests/, services/tests/EXPECTED_MIN_TESTS.txt, docs/balance.md, .sessions/, telemetry/model-usage.jsonl, control/claims/ · 2026-07-14

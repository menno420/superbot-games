# Claim · test — display-table completeness registry: vocab and display tables stay in sync

- `claude/night-display-table-registry` · **night-display-table-registry** — test slice implementing the display-table completeness registry card idea from `.sessions/2026-07-14-night-coverage-mining-core-naming.md`, verified unimplemented at main `f3fd346` (no test_display_tables.py anywhere; the ItemKind↔_KIND_EMOJI relation is covered only incidentally): one registry-style test module in the registered `tests/mining/` suite pinning every vocabulary↔display-table sync pair found by reading mining core — ItemKind ⊆ `_KIND_EMOJI`, `EffectiveStats` fields = `STAT_LABELS`/`STAT_GLYPHS` keys, `_CATEGORY_LABEL` values = `CATEGORY_ORDER` = `CATEGORY_EMOJI` keys, plus the sibling pairs the modules actually carry (`TYPE_EMOJI` keys ⊆ catalog base types, Biome↔`BIOME_LABELS`/`BIOME_EMOJI`, CellFeature↔`_FEATURE_GLYPH`/`_FEATURE_LABEL` + `MAP_LEGEND`, `_SECTION_ORDER`↔`SHOP_SECTION_LABEL`, `BRANCHES`↔`BRANCH_LABELS`) · expected files: tests/mining/test_display_tables.py, tests/mining/EXPECTED_MIN_TESTS.txt, docs/balance.md (floor bump regen), .sessions/, telemetry/model-usage.jsonl, control/claims/ · 2026-07-14

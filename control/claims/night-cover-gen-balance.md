# Claim · test — pin tools/gen_balance (generation, sections, helpers, stale detection)

- `claude/night-cover-gen-balance` · **night-cover-gen-balance** — test slice partially implementing the "gen_balance generator pins" card idea from #120's session card (`.sessions/2026-07-14-night-truth-stamp-anchor.md`), verified unimplemented at main `51590c6` (tools/gen_balance.py is 227 stmts at 0% coverage; tests/tools/ contains only test_stamp_scaffold.py): add tests in the registered `tests/tools/` suite that run generation in-memory / to tmp paths, assert `--check` passes against the committed docs/balance.md at HEAD, assert every expected section header is present in order, pin `_num` / `_table` / `_read_floor` behavior, and pin the stale-detection path (mutated copy → check() == 1 with a unified diff) · expected files: tests/tools/test_gen_balance.py, tests/tools/EXPECTED_MIN_TESTS.txt, docs/balance.md (floor bump regen), .sessions/, telemetry/model-usage.jsonl, control/claims/ · 2026-07-14

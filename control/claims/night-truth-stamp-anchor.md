# Claim · docs — machine-readable truth-stamp anchor + stamp scaffold tool + #108+ groom

- `claude/night-truth-stamp-anchor` · **night-truth-stamp-anchor** — three-part slice from #90's card idea + `.sessions/2026-07-14-night-truth-stamp-night-wave.md`'s card idea (both verified unimplemented at main `fdea103`: no `truth-stamped-at` marker anywhere in docs/, no `tools/stamp_scaffold.py`): (a) add a machine-readable `<!-- truth-stamped-at: <sha> -->` comment beside `docs/current-state.md`'s prose stamp; (b) new `tools/stamp_scaffold.py` reading that anchor and emitting `- **#N** (date, \`sha\`) — subject` bullets from `git log <anchor>..HEAD` (authoring aid, no CI gate) + a unit test; (c) the owed groom in the same PR — record #108 onward in `docs/current-state.md` (each verified against the git log) and move the anchor to the groomed HEAD · expected files: docs/current-state.md, tools/stamp_scaffold.py, tests/shared/ or services/tests/, docs/balance.md (if floors move), .sessions/, telemetry/model-usage.jsonl, control/claims/ · 2026-07-14

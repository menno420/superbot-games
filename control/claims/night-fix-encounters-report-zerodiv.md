# Claim · bug fix — games/mining/sim/encounters_sim format_report zero-actions guard

- `claude/night-fix-encounters-report-zerodiv` · **night-fix-encounters-report-zerodiv** — fix the latent bug found and pinned by #104's tests: `format_report`'s kind-frequency lines divide by `report.actions` without the zero-actions guard that `SimReport.encounter_rate` has, so `format_report(SimReport())` raises `ZeroDivisionError`; add the minimal guard mirroring the module's existing zero-denominator conventions and flip the pinning test from `pytest.raises` to asserting the guarded output; no other behavior changes, zero gameplay-constant changes · expected files: games/mining/sim/encounters_sim.py, tests/mining/test_encounters_sim.py, .sessions/, telemetry/model-usage.jsonl, control/claims/ · 2026-07-14
